# 第一页组合图与摘要核对

组合图位于摘要下方右侧，Introduction 在左侧环绕，图后恢复通栏。
原机制概览仍为 Figure 1。组合图使用可复现的 Matplotlib 矢量源，插入宽度
2.70 英寸，标签实际字号 8 pt；没有通过缩小字号解决碰撞。

雷达图使用 11 个任务、7 种方法的 77 个 Recall 原值。每轴范围由所有方法的
极值和统一留白规则确定，同一任务所有方法共享范围；图中明确显示端点。
VT/HotpotQA 的预算、指标口径和 FT K3 设置均保持不变。时间图保留 52 个成功点、
局部 min/max 误差条及 4 个 OOM 标记；未跨越缺失点连线。图内注明不同硬件，
正文说明本地 C550 与历史六/八卡来源。原始结果表、测试数据及全尺寸曲线均未改动。

先前居中放置的侧面雷达标签侵入圆周；修订后根据轴方向选择左右和上下锚点，
并为下方斜轴增加距离。新增检查覆盖文字与文字、文字与圆形绘图区（至少 2 pt
净距）、时间绘图区、OOM 标记以及画布边缘。最终仍需结合整页 PDF 渲染检查。

本次图形布局修订时，摘要按空白分词从 232 词压缩为 181 词，合并重复描述，保留以下信息：

| 信息 | 修订稿保留内容 |
|---|---|
| 解释对象 | 输入证据以及决定其使用方式的控制 |
| 干预和目标 | 原始/参考输入的有限干预，同一完整响应的对数概率变化 |
| 计算和守恒 | 解析算子规则、一次反向遍历、正负贡献之和等于目标变化 |
| 注意力 | 传递的值、选择变化，以及归一化保留的源间竞争 |
| 门控记忆 | 存储内容、key/query/gate 和保留/写入/读取 |
| 实现与复杂度 | FlashAttention tiling、FLA chunked computation；固定模型维度与块大小时辅助存储随序列长度线性增长 |
| 正式结果 | Qwen3-8B；多数任务最低 RISE/MAS；全部 NIAH 最高 recovery；较 FT 提升 3.96–19.60 个百分点 |
| 开发测量 | 单独的 Qwen3.5-9B 开发测量；混合模型实现；真实 batching 与 GPU checkpoints 带来 35.3% 吞吐提升 |
| 策略学习延伸 | 仍作为研究动机；精确平均动作级回报对比恢复 advantage |

可读稿 `abstract_introduction.md` 的摘要与 LaTeX 同步。最终 PDF、分页、渲染检查和
文件哈希记录在 `../build_receipt.json`；图的源数据与碰撞检查记录在
`../figures/intro_teaser_manifest.json`。

后续正文清晰度修订已同步摘要与 Introduction；当前版本及页数以 `clarity_revision_review_zh.md` 和 `../build_receipt.json` 为准。本记录保留图形布局修订当时的检查结果。
