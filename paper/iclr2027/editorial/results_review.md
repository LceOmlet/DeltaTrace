# Results and PDF review - 2026-09-11

- The merged quality tables contain all seven methods: 13 tasks for RISE/MAS and 11 for recovery. VT budgets are 10%/10%/20%/30%; HotpotQA reports supporting-fact Recall at a 10% body-token budget. MATH and MoreHopQA recovery remains unavailable.
- The combined CSV has 91 method/task rows and 259 metric values. All 224 historical values are preserved; 35 verified VT/HotpotQA values are added. A separate summary reports seven VT macro values. Budget, metric unit, target scope and method variant are explicit in the CSV. The main text and appendix define the recovery targets, units, and budgets.
- The independent result check compares every formatted table cell, budget and boldface winner with source records. DT has the best observed mean on 9/13 RISE tasks, 11/13 MAS tasks, and 10/11 recovery tasks; IFR is highest on HotpotQA. These are observed means without a significance claim. No model calls are required for this integration.
- Figure 1 retains the approved wider forward/reverse trace and vertically stacked Attention/GDN panels. The clarity revision rebuilds both overview variants to separate forward activation changes, local change identities, and reverse coefficient updates. All overview source data and signed case scores remain unchanged. The two existing case figures and first-page inset retain their assets.
- Both case figures retain their exact tokens, signed scores and model-matched DT/FT deletion curves. The rollout curves now share a first-page inset with an eleven-axis Recall radar. All 52 successful timing points, local min/max error bars and four OOM crosses are retained. The original full-size asset remains available. The inset identifies mixed hardware; the evaluation describes the local and historical timing scopes. Existing implementation-cost records remain in the text and appendix.
- The manuscript is rebuilt from the merged sources. Final pagination, visual review and hashes are recorded in `../build_receipt.json`; source checks are in `../source_verification.json`. The merge preservation receipt records exact Git object comparisons for the incoming test data and retained figure assets.

Verification: `../results/verification.json`, `../figures/overview_layout_verification.json`, `../source_verification.json`, `../build_receipt.json`, and `../merge_verification.json`.

The clarity revision explains the tasks and metrics before interpreting the same results. It identifies the timing model, per-example median aggregation, and serial/batched comparison, removes development-history prose, and adds no experimental settings inferred from implementation code. See `clarity_revision_review_zh.md`.
