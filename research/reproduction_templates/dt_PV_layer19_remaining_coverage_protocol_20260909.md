# PV19 fixed remaining coverage

Prepared only. Root decides whether to continue after the existing MH0/NI1 pilot. This document and manifest launch no work and record zero new model/DT/scorer/FT calls.

The remaining original cases are fixed in this order: (1) NI0/MH2, (2) NI2/MH1, (3) NI3/MH3. Each segment uses one process, with control then candidate on its first case and candidate then control on its second. The control keeps layer-0 symmetric normgate and FLA endpoint average; the only candidate change is layer-19 PV content0. No later case, layer, weight or rule selection is permitted.

Each dispatched segment is bounded by 4 DT, 100 finite FLA backends (200 native adjoint stages), 32 finite FA, 32 public FA LSE and 84 original scorer calls, with zero FT recomputation or generation. All three remaining segments would total 12 DT / 300 FLA / 96 FA / 96 LSE / 252 scorer calls. These are preparation limits, not actual costs; initialization, failures, elapsed time and memory remain explicit in run receipts.

Report every method's complete original 21-point curve and original needle/RISE/MAS metrics, every paired regression, and descriptive means with the actual completed coverage denominator. Also contract both new vectors on the same new control deletion sets against its actual raw score curve; this ledger requires no new scoring and is not a new MAS curve. Retain raw signed density, un-clipped response/errors, mask/input hashes and source-vector/endpoint drift. Lower RISE and MAS is better. NIAH needle is reported from unchanged author gold; MH has no NIAH needle gold.

A material paired regression stops subsequent segments, with the observation and root decision recorded. No invented numerical tolerance or significance threshold is imposed. Unrun cases remain incomplete, and improvement on a partial set cannot be called an eight-case pass.

The manifest binds source records, prompt/target hashes, original cached spans, actual input/target IDs and baseline hashes, and author gold mapping to the completed original eight-case snapshots. Three originally null input/gold expectations are explicitly bound to those completed actual receipts, not described as originally pre-pinned. Earlier FT measurements are historical same-input references: new and historical native score endpoints may drift across processes, so they are not automatically matched new FT comparisons. This is development coverage, not a held-out, universal or real mini-batch validation.

Manifest SHA-256: `bdbffb3cf486295215cf1318cf89897cfb56d06f72a59224ec8e2bf4549819fa`.
