# Prepared only: no whole-model result

This directory preserves the exact frozen supported-secant pilot payload.
It has not been dispatched, so there are no model, attribution, scoring, timing,
or quality results for this proposed whole-model experiment. Its two cases and
budget are a prospective protocol, not evidence of a successful repair.

The copied runtime predates the optional PV19 selector. This is intentional:
rebuilding against a later runtime would change the frozen source identities.
The builder is historical preparation code and may refuse a later source tree.
No automatic launch or follow-on test is authorized by the preparation marker.

The completed PV19 work found a conditional-amplitude versus ranking tradeoff.
A next candidate should be justified against that evidence before execution.
The pure-PV interpretation of swapping endpoints requires the current symmetric
logmean/QK backend. An asymmetric supported-secant backend also changes its
Jacobian reference when endpoints are swapped; do not combine the two silently.
