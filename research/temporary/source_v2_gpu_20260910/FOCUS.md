# 用户指定的 VT / HotpotQA 聚焦补充协议

2026-09-10，用户在 VT H4-C1 完整结果之后明确要求暂停 NI 重测，专注 VT 和 HotpotQA，公平验证 DT 的优势。本补充文件记录范围变化，不改写首次 GPU 调用前的 PROTOCOL.md。

- 范围改为 VT H2-C3、H4-C1、H6-C1、H10-C1 各 100 例，以及 HotpotQA 48 例，共 5 个任务、448 例。NI 不再进入执行队列；先前 NI 前缀继续保留为已中断记录。
- 已完成的 VT H4-C1 和 HotpotQA 原始结果按哈希复用，接续另外三个 VT。原模型、原输入、两种 DT reference、FT K1/K3 归因、正文范围、预算、排序和删除函数均保持不变。
- 补齐 FT K3 的 RISE/MAS：使用每例本次保存的 FT K3 向量，在同一原模型上实际执行同一正文删除序列，不复用 FT K1 的曲线代替。每任务第 0 例重复 FT K1 删除作为跨阶段一致性检查。
- 比较两种 DT 与 FT K1、FT K3 各自的 Recall@10%、RISE、MAS，所有正负结果保留。多预算及完整句/行检索继续作为次要指标，实际 token 成本单列。
- 每任务报告配对差及 95% bootstrap 区间；5 任务宏平均固定等任务权重。另对宏平均中两种 DT × 两种 FT × 三项指标共 12 个比较报告联合 bootstrap 同时区间，避免从多个比较中仅选择偶然有利的一项。
- Bootstrap 固定 10,000 次，seed=73，在任务内按整例配对重采样。已看过的 VT H4-C1 和先导样本明确作为已观察数据；报告为这五个固定任务上的补充实验，不声称独立盲测或推广到全部 11 个任务。
- 支持优势的条件是共同评测规则下对应指标的配对收益稳定；恢复率或删除指标有退步时同时报告。测不到优势也保留结论，不调整 gold、候选范围、预算或 FT 参数以追求有利结果。

本补充协议在首次新增 FT K3 删除评测之前保存。执行回执记录其哈希、程序版本和结果来源。
