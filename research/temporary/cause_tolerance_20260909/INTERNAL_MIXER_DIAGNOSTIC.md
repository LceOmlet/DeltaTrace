# Separate normalization from finite core allocation on actual deletions

The full matched trace and the output-gate pilot are completed. The latter
rejects gate-only priority: NI gate/norm-gate terms oppose the excess preference,
while the remaining FA/GDN terms are positive. It does not tell whether internal
normalization geometry, route/content allocation, or the other input branches
cause the difference. No quality rule is selected by that account alone.

Freeze the same original NI0/1 and MH0/1 10% DT/FTK1 deletion sets. Add passive
observations of actual native core inputs and actual Q/K norm inputs/outputs.
FA supplies its real query, key and value at the official interface. Native FLA
supplies raw Q/K and decay at entry, and its actual normalized Q/K, value and
beta at the existing chunk-stage return. Do not reconstruct probabilities or
write a recurrent forward. Preserve original targets, EOS controls and all
same-process plain/observed full-vector comparisons.

The own controller receives only one diagnostic addition: it passes its actual
RoPE operands to the observer. The diagnostic runner shares all baseline
finite callables and configuration; no native model/module method is replaced.
The ordinary runner remains the control. Record new source hashes and exact
derivation from pinned previous sources before launch.

FA raw Q/K finite coefficients are internal to an existing compiled composite.
Re-evaluate the same existing normalization expression for diagnostics, then
reconstruct its original input transpose with the existing three vendor GEMMs.
Report the resulting difference from the actual compiled input coefficient as
a separate term, not as normalization or route error. GDN raw Q/K coefficients
are re-evaluated with its same eager finite helper and must exactly reproduce
the existing grouped coefficients. These are extra diagnostic operations,
not alternative model/attention/backward implementations or free work.

Split each remaining mixer into Q normalization, K normalization, RoPE rounding,
finite FA/FLA core, FA upstream BF16 cast, compiled-boundary reconstruction, and
the projection/conv/other remainder. Keep the previous output gate/projection
and GDN cast terms separate. All signed pieces must telescope to the unchanged
whole-mixer error and full input-to-output deletion error. Keep both positive
and opposing pieces and the successful MH controls. A large error only supports
further causal testing of a simple uniform rule; it cannot certify such a rule.

Pilot cost:8 complete DT,8 paired partial native forwards,1 native initialization,
1 model load. Extra finite diagnostics are32FA output projection transposes,
96FA input projection transposes,64FA norm evaluations and192GDN norm evaluations.
There is no candidate scoring, no new generation, no FT rerun and no production
timing claim. Compare preceding-process observations with reported drift rather
than imposing bitwise identity across jobs. Stop if the same-process control or
provenance fails; do not modify the official frameworks to make it pass.

The matched Qwen3 pilot uses the same four author indices and its own original
fixed deletion sets. Its normalization coefficients are already explicit at
the existing callback boundary, so retain those actual coefficient references
without re-evaluation. Observe original Q/K norm modules and the public default
FA call's Q/K/V/output; do not call attention again. Sum GQA coefficients using
the existing layout and keep RoPE rounding, upstream FP16 cast, output projection
and the input-projection remainder separate. Only read-only callbacks are
added to the pinned own finite propagation, with an exact source derivation.
This pilot costs8 completeDT+8 paired partial forwards and1model load. There
are no additional model or finite-normalization/linear-transpose evaluations.
