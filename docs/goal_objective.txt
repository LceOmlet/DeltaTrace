继续研究两种干净DeltaTrace（Qwen3-8B与Qwen3.5-9B）的质量根因和容差加速。保护已发布的Qwen3论文全表标签qwen3-8b-table1-clean-v1-20260909及固定FT实现，在独立研究分支推进。首先用作者原数据、相同样本索引和冻结生成轨迹建立两模型的可比证据，RISE保留有符号排序，MAS与needle沿用正值视图；分别观察DT相对同模型固定FT的差距，不用跨模型绝对指标或Qwen3全表对Qwen3.5八例直接归因。优先找最明显的实际错配，区分评测/目标及输入差异、数值或实现错误、GDN共享归一化key和内容/控制交互规则的结构性问题、模型与任务本身差异；已有算子反例不是实际任务主因，已失败的统一归一化候选不无依据重跑。每项候选先写清可证伪机制、计算核心、预期影响和无额外复杂度的修法，再以原生实现作局部核验和固定原样本对照，量化它解释了哪些删除曲线/排序差距；只接受简明、统一、可泛化的修复，禁止按例、层号或答案数字标签补丁，禁止堆积分步数、端点或扫描规则追指标。有效候选覆盖原NI0–7/MH0–7全部16例并用归因前冻结的其他作者任务样本验证needle、RISE、MAS稳定性；无效候选及时停止，保留失败证据，不能保证胜出或为得分改FT。分别推进Qwen3和Qwen3.5 DT的容差加速，先依据真实完整调用剖析找大头，优先复用默认FA/FLA、厂商实现、原生批处理和官方编译器，接受默认精度，不造影子注意力/模型/反向，不修改框架追微差；避免全局序列平方显存及明显复杂度退化。Qwen3.5以已测正式任务内B2成本15.960→12.403秒/16例、21.644→23.628GB为起点，Qwen3另立同输入的实测成本基线，不移用跨模型加速倍率。质量修复与数值加速分开试验及归因，报告全部逐例退步、跨进程数值变化、完整延迟、真实样本mini-batch吞吐、峰值显存和冷启动；GPU计时串行，计算期间继续分析和准备必要验证。交付可审查的差距与错配证据链、根因支持/反证、修复或停止结论、两模型质量及效率取舍；持续提交推送DeltaTrace并验证备份。仅创建Goal或启动测试不算完成。
