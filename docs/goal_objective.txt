恢复并冻结 Qwen3-8B 和 Qwen3.5-9B 的干净 DeltaTrace，撤下因误把有符号 MAS 差距视为传播错误而添加的逐层修补。明确原 MAS 的响应截断及单调化不适合直接验证完整有符号贡献；RISE保留有符号排序，MAS单独取正值视图，needle保持冻结正值视图，均调用作者原指标，另存原始有符号向量，禁止把正值指标改善解读为负贡献错误。两种模型各覆盖作者处理好的 NI0–7、MH0–7 共16例，固定输入、生成轨迹、eligible/gold 映射及同模型原版 FT 对照，报告原 needle、RISE、MAS、所有逐例退步和版本差异；仅复用已核验完全符合干净配置的历史结果，带补丁版本不能算干净验收。先固定提交干净方法与作者原表评测入口。正式实验按原表对应模型、完整发布缓存、输入包装、目标和原指标运行DT，直接复用FT原论文发布的CSV，不全量重跑FT。16例仅作开发回归；加速在独立分支与质量覆盖并行，依据完整归因耗时和显存大头研究容差加速，优先复用默认 FA/FLA、厂商及官方加速实现，接受默认精度的合理数值差异，不追逐位一致，不构造影子注意力、模型或反向，不叠加复杂归因规则。用相同16例质量、完整延迟、峰值显存及真实样本 mini-batch 吞吐验证取舍，加载、编译和预热单列；避免全局序列平方内存及显著复杂度退化。测试期间继续分析瓶颈，持续提交推送 LceOmlet/DeltaTrace 并验证恢复备份。 只采用简明、可泛化、实际验证有效的错误修法，不按样本或层号补丁；区分结构性反例与真实任务主因，统一归一化候选未改善NI RISE时停止扩测，不进入干净方法。RISE/MAS视图修订的4例入口验证不得冒称原16例或完整原表完成。
