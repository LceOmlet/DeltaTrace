# 当前执行目标：固定干净代码与FT原表评测，并行加速

2026-09-09，用户明确替换研究主线。旧的以有符号MAS差距驱动传播修补的主线已被撤回；不再根据单层、单例残差叠加候选。最新完整正文见[goal_objective.txt](goal_objective.txt)；应用中的既有Goal仍在执行，工具不支持直接改写其正文。

**当前执行：两模型干净DT及作者原表评测已固定，两模型各16例开发回归已完成；正式实验运行DT并直接复用FT发布的原始CSV，容差加速在独立分支推进。** 当前配置：[clean_dt_baselines.json](../configs/clean_dt_baselines.json)。[32条结果及全部逐例退步](../research/temporary/development16_20260909/README.md)已保存；完整原表DT实验尚未执行。带修复项C的结果只作历史记录。

用户最新要求优先固定提交干净代码及正式评测，加速并行推进。干净方法入口移至`deltatrace/clean/`；正式评测位于`experiments/official/`，按作者Table 1发布协议固定输入包装、eager评分、忠实度FT K=1和needle FT K=3。临时诊断及未对齐草稿位于`research/temporary/`。原16例只是开发覆盖；直接比较原论文已有均值须使用同模型、同任务完整发布缓存。不能用披露差异替代对齐。

## 1. 干净实现与统一口径

- 先纠正指标适用性：原MAS把模型响应截到[0,1]并强制单调不增，却没有对有符号剩余归因作同样处理，因此不能直接验证完整有符号贡献。以完全正确的加性贡献+2、−1为例，删掉+2后真实与预测剩余均为−1；响应被截成0，局部对齐罚项却为1。这是对齐项的反例，不是声称最终MAS恰好等于1。此前据有符号MAS差距认定传播错误的结论撤回；裁负改善也不能证明负贡献错误。见[原实现](../research/third_party/flashtrace_qwen35_e81b3be/flashtrace/improved.py)。
- Qwen3-8B：恢复原16例已经使用的P1有限归因与可追溯FA扩展，沿用原简单规则。
- Qwen3.5-9B：使用原`Qwen35DenseFiniteRunner`默认规则，四个可选规则映射均为空；撤出第0层norm-gate修法、第0层额外FLA平均、PV19、K归一化等针对MAS的层级改动。FA/GDN保持原content1规则。原生模型FA/FLA不改；24次有限FLA、48次原生伴随，不能把带25次FLA修复项的结果混入。
- RISE/MAS统一取输入归因的正值部分，再交给作者原函数；原有符号向量单独保存。针对于本任务，正值指标为主；不再把MAS当负向方向验收，也不扩展强化学习需求。
- 固定原FT，禁止修补或重写FT。两模型各自比较同一实际权重上的原FT，明确历史版本、输入包装和目标差异，不用跨模型分数替代同模型对照。

## 2. 与原Qwen3同规模的测试

两模型都覆盖作者NI0–7、MH0–7，每模型16例。冻结源码、原缓存、完整生成轨迹、tokenizer及eligible/gold映射，再执行或核验复用。完整报告原needle、RISE、MAS和所有逐例退步；MH没有gold则needle记不可用。它们都是已用开发样本，不冒称独立确认或论文原表复现。

已有结果只有在源码/配置、实际评测向量、输入/目标、后端和指标口径匹配时才可复用；这是缓存身份核验，不是给优化设置逐位一致门槛。其余重测。带修复项C的八例正值结果不构成干净Qwen3.5验收。固定全部16例，不能根据改善挑例或更换范围；出现源码/输入错误先修测试入口，不通过修改FT或调方法掩盖问题。

## 3. 独立分支并行研究容差加速

冻结方法和正式评测后，加速在独立分支推进，与质量覆盖并行；不覆盖已引用的固定版本。分析完整归因的实际大头，优先复用默认FlashAttention/FLA、厂商算子和官方编译器。接受FA默认精度下合理数值变化，以实际质量和计算收益决定容差；不为微小差异修原框架，不增加方法复杂度、积分步数或端点来追分数。同一GPU上的计时实验串行调度，避免并行负载污染成本。

同时报告完整归因延迟、每样本摊销、峰值显存及真实mini-batch吞吐；加载、首次编译、预热和诊断分开记账。EOS/输入两端点不算两条样本。显存以相同设备、模型、长度、后端和batch下的默认FA执行为参照，不设固定额外MB硬上限。避免全局序列平方阶内存和显著计算复杂度退化。每个有价值优化仍接受同一16例原指标回归，不要求逐位一致。

## 4. 执行与交付

测试时同步分析已有规律、计算瓶颈和准备下一项必要验证；不干等，不展开局部残差驱动的层扫描。保留全部历史源码和正负结果，当前入口由[active_profile.json](../configs/active_profile.json)明确指向干净配置。每个阶段提交推送LceOmlet/DeltaTrace，核对远端并验证Git bundle。

完成条件：两模型干净基线的16例原质量明确、执行路径可追溯，并得到可复现的容差加速/显存/真实批处理取舍。只有修改goal、选定配置或带修复项的八例结果均不算完成。
