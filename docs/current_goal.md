# 当前执行目标

2026-09-08 更新。本文整合用户追加要求，是当前执行入口；旧报告和已执行协议保留为历史证据。出现冲突时以用户最新指令及本文为准，不将旧协议的失败追溯改判为通过。应用中的研究 Goal 继续进行，尚未完成。

**应用 Goal 与本文的关系：** 当前工具不能修改进行中的 Goal 正文，因此应用仍显示创建时的旧目标。本文保存并落实后续追加要求；旧正文中的方法优先级、保留集划分及成本要求若与用户最新指令冲突，按本文执行。没有将未完成目标标为完成来重建 Goal。

目标仍是在 FlashTrace 原解释目标和原评测下，得到解释更有效、符号定义明确、实际成本可接受的方法。继续优化已有 content_P1 有限传播方法；不要求必须使用 VJP，不扩展强化学习任务。仅加速 FT、模型加载成功或局部数值检查通过均不构成目标完成。

**当前最高优先级：停止修改官方 FT，恢复可信的原版对照。** 不再接入 AST 提取的 Both 控制器，不再改造 FT 内容分配。现有9B core0—3及全轨迹诊断是本项目自定义FT衍生变体，不能作为完整官方FT的成绩。先核对完整未修改官方入口与实际运行证据，再决定有明确预算的原版小样本验证；不先修原版再声称验证原版。详见[本次纠正](history/停止FT改造并纠正对照身份_20260908.md)。97次B2删除评测继续暂缓。

## 优先级与验收

1. **先审查剩余 FA 收益，再转向其他主要耗时。** 用同一工作负载的算子剖析和完整归因配对计时判断剩余收益，记录预期收益、调用预算和停止条件。证据支持收益有限时，按实际成本处理层重放、投影/MLP、输出头、搬运或调度；不必穷尽所有 FA 微优化，也不能由一个失败候选推断全部 FA 空间耗尽。局部耗时、内核数及跨实验百分比不能冒充整网收益。

2. **保留默认模型 FA，有限传播扩展复用可追溯框架。** 优先使用已有官方/厂商算子、公开接口和编译器；必要扩展须说明上游版本、修改边界、数学规则和真实调用证据。禁止另写影子注意力、模型或 backward 冒充原生实现。双端点混合乘积仅用于归因恒等式，不冒充真实反事实前向。生产归因路径避免物化全局序列长度平方阶矩阵。

3. **完成 Qwen3.5-9B 与真实 FLA 的归因支持。** 复用远端共享权重，核验官方来源、模型类和 tokenizer。标准注意力走模型默认 FA，线性注意力走实际 FLA；端点、门控、状态递推和有限传播规则须符合新模型结构，不能直接照搬旧模型 PV 规则。验收依次覆盖真实模型执行、固定轨迹及 gold/token 映射、有限传播接入、真实异长批处理、原指标和完整成本。导入/前向成功不等于整网归因支持完成。

4. **效率、显存和批处理共同验收。** 保留 FA 常用默认精度，不追求逐位一致；检查数据流、数值影响、符号涉及的贡献、排序和原指标。效率不能严重退化，不能用不断增长的查询/积分次数换取质量。显存以相同模型、设备、FA 后端、精度、长度和实际 batch 的原生执行为参照，不设“一次反向 + 72.5 MB”等固定上限；同时报告速度、吞吐、峰值及可支持的长度/batch。支持真正的 mini-batch，不能把串行循环称为批处理。首次编译、诊断 hook 和稳态成本分开记录。

5. **只沿用官方评测，先做有决策价值的小预算验证。** 使用作者处理好的固定轨迹或严格官方生成流程，沿用原 needle、RISE/MAS 和 gold；不自行生成替代 benchmark。主对照明确为同一实际Qwen3.5权重上的FT混合架构变体；当前共享模型标识Qwen3.5-9B，不混用Qwen3-8B。候选与 FT 在同一实际模型、轨迹和可归因范围下比较，并核对 FT 是否计算了无关输出。Qwen3.5 结果属于模型扩展，不能冒称论文原表复现。已被历史实验使用的样本不能重新称为独立确认集。达到可比较状态后优先原指标和方法效果；不继续修作者框架，不以约2×10⁻⁶等微小舍入或非逐位一致延迟比较。先冻结预算与停止条件，部分结果足以改变决策就停止扩跑；不为置信区间跨零强行调参或追加样本。

6. **持续提交推送与可恢复交付。** 在 LceOmlet/DeltaTrace 保存公式、源码、来源/产物哈希、冻结协议、正负结果和下一决策；阶段完成后提交、推送、核对远端提交并验证 Git bundle。开发草稿标注未执行或未验证，不能只保存在远端临时目录，也不能混入已验证成果。

**本次追加要求的执行顺序：** FA 剩余收益的证据足以支持转向时，停止低收益微调并处理其他主要耗时；Qwen3.5-9B 必须完成默认 FA 与真实 FLA 的整网有限传播和异长批处理，不能仅以权重加载或局部算子通过交付；每个完成阶段提交、推送并验证可恢复备份。具体实验仍先冻结调用预算和停止条件，不因 Goal 进行中而自动重复已关闭实验。

## 当前证据与下一步

**用户最新约束覆盖以上工程安排：官方 FT 基线不因本项目的 FA/FLA、批处理或低显存目标而被重写。** 原版的实际后端、batch与成本如实记录；DeltaTrace自身继续承担其工程目标。不得以相同底层内核、局部重构或重放同一自定义聚合结果代替完整原版验证。

| 当前问题 | 已确认 | 下一动作与边界 |
| --- | --- | --- |
| DT目标迁移 | 同9B/同端点/同11个运行模块，原NI0从1/40到16/40；旧8B同例17/40。 | 保留单例回归，不据此宣布优于官方FT。原答案适配仍未通过证据恢复验收。 |
| 自定义FT目标诊断 | 同一32份原生缓存，单次全轨迹聚合由2/40到8/40；实际8FA/24FLA/24conv反向。 | 保留事实，但撤销其官方基线资格；当前core0—3也使用自定义内容分配。两者都不能代表完整官方FT。 |
| FT版本与实现身份 | 官方075e7e4 Both、e81b3be Both与现有core控制规则不同；已有改造还改变了内容分配。 | 停止控制器移植与内容改造。先查完整未修改官方入口、固定版本和实际运行证据，不用自定义聚合结果自证等价。 |
| 本轮资源 | DT97.83秒、FT诊断34.31秒；30次新增decoder重放，零新整网root、零生成、两次原CPU needle。 | 两个预算关闭且任务终止。流式权重/缓存、冷调用、诊断IO不代表公平稳态生产成本；不因小残差重开算子打磨。 |
| 后续评测 | 仍用原作者固定文本、gold、eligible和指标。NI0/MH1均为历史开发样本。 | 只在原版身份明确后制定小范围运行预算；原97次B2删除评测继续暂缓。撤回的Both草稿不执行。 |

当前完整说明见[目标回归实测与FT版本语义](history/Qwen35目标回归实测与FT版本语义核验_20260908.md)。原[共同失败诊断](history/Qwen35共同归因失败与目标错位诊断_20260908.md)、[core0—3](history/Qwen35同模型FT全跳对照与首个原指标_20260908.md)及[原指标暂缓预算](../configs/qwen35_original_metrics_budget_20260908.json)保留，不将旧失败改判为通过。

下一执行入口：**完整未修改官方 FT 的入口与既有运行证据核查**。禁止继续接入撤回的 Both 草稿或把自定义 FT 变体包装为官方对照。当前40%/20%分别是DT单例回归与自定义FT诊断，不能用于判定对官方FT的胜负。

完成条件仍是：实现来源与数据流可信，在所声称范围内确认原评测质量优势，并支持明确的符号解释与可接受成本。上述工程里程碑仅是必要步骤，研究 Goal 保持 active。
