# MAS条件错配修复：第0层norm×SiLU

2026-09-08。执行用户“把这个MAS所指示的错配作为goal，修好”。应用Goal已创建并激活。本次不止建立计划：已完成两个有原指标的实际配对修复实验，接入正常Runner选项；成本验收单独执行。Goal尚未完成。

## 修了什么，为什么先修这一处

记GDN输出为 `y = n * s`，其中 `n = RMS(o)`、`s = SiLU(z)`。0是允许归因的prompt位置替换为EOS的端点，1是原输入；固定回答和非归因位置保持原值。

原规则 `Δy = s1 Δn + n0 Δs` 将全部乘积交互分给内容分支。实际部分删除时，内容和门一起变化，而门分支仍用EOS端点内容。此前同一实际删除集合已定位到第0层norm-gate和FLA的明显过估，以及低EOS归一化半径下的方向放大；不能由端点恒等式推断部分删除预测也正确。

本次唯一方法改动是**第0层**：

`Δy = ((s0+s1)/2) Δn + ((n0+n1)/2) Δs`。

它平分交互，不改RMS有限传播规则、不改FLA有限传播规则、不调整最终分数，也不裁掉负贡献。两式都保持有限端点关系；对任意部分删除都精确并不是它们的性质。预期收益来自改变流向内容与门的上游系数，从而减少已测出的条件放大；这必须由完整输入与原指标检验。

正常调用为 `Qwen35DenseFiniteRunner(..., norm_gate_rules={0: 'symmetric'})`。未配置时保持原content1路径，便于复现。仅添加线性规模的端点运算；FA/FLA、卷积和模型调用数不增加。这个复杂度理由不替代实际计时。

## 原评测结果

使用作者已有NI1及MoreHopQA1固定文本与生成轨迹，当前/候选解释同一真实模型前向和同一上游系数，各按自己的分数排序调用未修改的原20步指标。RISE和MAS均越低越好。Qwen3.5-9B属于模型扩展，不冒称论文原表复现；两条都是历史开发样本。

| 样本 | RISE 原→修复 | MAS 原→修复 | needle 原→修复 | 固定FT的MAS |
|---|---:|---:|---:|---:|
| NI1 | 0.038548→0.037258 | 0.288765→0.198629 | 35/38→37/38（92.1%→97.4%） | FT0–3：0.247399–0.250078 |
| MoreHopQA1 | 0.158529→0.152059 | 0.338220→0.269516 | 无needle gold | FT0：0.267664；FT1–3：0.281373–0.292056 |

NI的MAS下降31.21%，低于本例所有固定FT变体；MH下降20.31%，仍比FT0高0.001852。不能写成“两例均胜过全部FT”。固定FT采用已保存、输入与来源核验过的结果，本次没有重新运行或修改FT。

同一预先冻结删除集合的完整输入误差（预测−实际）如下。它们不是候选重新排序的集合，因此能直接检验传播改变，而非排序选择效应。

| 样本 / 删除步 | 实际变化 | 原误差 | 修复后误差 |
|---|---:|---:|---:|
| NI1 / 1 | 113 | +10.2622 | +6.1483 |
| NI1 / 10 | 161 | +47.9548 | +9.2659 |
| NI1 / 20 | 155 | +0.0630 | −0.0143 |
| MH1 / 1 | 45 | −1.2920 | −1.6669 |
| MH1 / 10 | 72 | +25.4716 | +16.6767 |
| MH1 / 20 | 85 | −0.6950 | −0.6764 |

**保留负面证据：** MH早段绝对误差增加0.3749。NI端点decoder舍入残差也未严格相同（约0.0686→0.1459）；这不是逐位守恒修复。局部组之间仍会抵消，不能用局部误差接近零或末端点好看宣称全部条件效应准确。

NI中段完整误差减少38.6889，其中第0层norm-gate减少6.9372、FLA投影误差减少30.0348，其余第0层项合计减少1.7169；第0层以外（含输出头、末端归一化等）的4.1124误差保持不变。**FLA规则没改**，其误差投影变小来自norm-gate下传系数改变。这是针对传播根因的干预证据，不是“修了FLA内核”的证据。

MH中段修复后，第0层误差约3.0478，而第0层以外（也含输出头、末端归一化等）的剩余误差仍约13.6289。剩余问题的优先级应随这一实际位置改变，不能继续仅靠第0层beta调节去追总分。

## 用户提出的beta内容错配确实存在，但不是本例最大头

`u=beta*c, c=v-r`，原分配 `Δu=beta1*Δc+c0*Δbeta`。相对于部分删除状态A，局部预测−实际的理论项为 `(c0-cA)*(beta1-betaA)`。完整图还包括输入c等分支自身的预测误差，不能拿这个局部恒等式代替总归因误差。

保存实际FLA端点、部分删除状态、实际系数L及运行时r0后，执行了5.59秒CPU分块审计。没有新的模型/FA/FLA调用，没有另算替代状态轨迹，也没有全局序列平方矩阵。CPU读出只是局部数学诊断，未冒充native内部r。

| NI1位置 | 理论beta错配 原→修复 | 含数值项的局部写入误差 原→修复 |
|---|---:|---:|
| 早段 | −0.47113→−0.20789 | −0.45382→−0.20050 |
| 中段 | −0.75720→−0.70357 | −0.80343→−0.74471 |

中段完整FLA原误差为**+36.16255**，beta理论项却是**−0.75720**。它在这个目标上的净作用反而抵消正误差，不能解释主要过估。不是说beta普遍不重要，也不是否认用户公式。

原生写入与局部数学重构存在0.349%–0.614%的相对L2差，目标投影单列；例如中段原规则的该项约−0.04454。四项分解、组/head汇总闭合误差小于1e−13，不将原生精度误差藏进方法项。直接把c0换成c1且保留beta1会重复分配交互，不是此次修法。

## 来源、预算与复现

FT固定 `e81b3be50a48dcfc652fbf1b530069b552736e66`。原模型、默认FA/FLA及权重前后身份一致。NI/MH的17个计算源码完全相同；第二条只改变数据选择和状态说明。旧MH向量2%检查失败保留，本次采用新冻结的同次实际端点配对依据，没有追溯放宽旧实验门槛。

两次GPU任务实际各1次模型加载、1次原初始化warm、1次完整DT、32层重放、24次当前有限FLA和1次额外第0层候选FLA、46次原评分；合计92次评分，0次FT、0次生成、0条新数据。任务分别70.89秒和63.58秒。配对候选还含额外GDN卷积/投影、诊断输出保存等成本，**这些耗时不能当正常候选速度**。

完整原指标、排序与全部删除集合、FP32密度、needle映射、来源和调用记录由[独立审计](../../evidence/verify_dt_normgate_repair_original_metrics_20260908.json)重算。[beta审计](../../evidence/verify_beta_content_mismatch_20260908.json)保留其验证范围。

原始结果：[NI1](../../evidence/dt_normgate_repair_NI1_20260908_v1/results.json)、[MH1](../../evidence/dt_normgate_repair_MH1_20260908_v1/results.json)。冻结协议与源码：[NI1](../../research/reproduction_templates/dt_normgate_repair_NI1_20260908_v1/protocol.json)、[MH1](../../research/reproduction_templates/dt_normgate_repair_MH1_20260908_v1/protocol.json)。私有实际激活和系数按哈希保留，不公开模型权重、激活或完整token文本。

## 正常路径与成本验收

随后完成了冻结的8次正常归因（NI首次C/S、计时C/S/S/C、MH首次C/S），`observer=None`，使用正常编译FLA后端。没有配对候选重传播或L/r0诊断捕获。实际8次原模型根前向、256层重放、192次有限FLA、64次有限FA及64次辅助FA，0次新模型评分；整项任务63.13秒。

| NI正常路径，完整归因 | 原规则 | 对称第0层 |
|---|---:|---:|
| 后续两次耗时，秒 | 1.3382 / 1.3337 | 1.3379 / 1.3521 |
| 中位耗时，秒 | 1.33590 | 1.34498 |
| 峰值allocated，字节 | 20,396,576,768 | 20,455,854,080 |
| 运行前allocated，字节 | 18,886,873,088 | 18,886,873,088 |

观察到耗时+0.68%、峰值+59,277,312字节（56.53 MiB）。这支持“本次修复没有明显成本恶化”，不支持统计提速保证或所有长度相同代价。首次NI调用8.3941/1.3644秒、首次MH形状4.9091/0.6641秒保留，不能以不对等预热的首次时间宣称加速。编译缓存沿用既有目录，不称为空缓存测试。峰值包含权重、实际模型及当前CPU检查点/重放路径；真实多样本batch仍未验收。

NI归因相对配对版有约7.14e−7/4.51e−8的FP32评测向量差异，全部删除后的完整输入仍一致。按输入哈希复用之前真实评分，原函数为每个新向量重算自己的density/metrics；正常版本MAS为0.288765001→0.198629090，RISE及needle与配对版相同。缓存标量读取不算新模型评分，也不冒充原生token级logprob。

MH在同进程先运行NI长度后，原/候选归因相对独立配对版分别变化2.0221%/2.1614%，共同root效应变化0.16972，删除集合也改变。该项没有获得缓存指标复用资格；已另冻仅对保存的两条正常向量补42次原评分、0次DT的协议，不修改原生框架或回退初始化来强求旧向量一致。

## 当前验收边界

已完成的是：一个根因明确、完整传播和原指标均有改善的最小修法，正常Runner入口、NI正常路径及实际成本验收。MH正常向量的新排序原指标已补完，见下节；历史FT不能直接与本次新评分比较。随后按修复后的最大实际误差定位，保留MH早段反例。当前没有独立泛化、真实多样本batch或整网提速结论，Goal继续执行。


## MH正常向量补测已完成

仅使用正常路径保存的run6/7向量，完成42次原B1评分、0次DT、0次FT、0次生成；任务43.39秒。每次实际输入均匹配正常路径的新删除集合。原函数重新计算得到：

| MH正常路径，同次原评分 | 原规则 | 修复后 |
|---|---:|---:|
| RISE | 0.149260355 | 0.139792899 |
| MAS | 0.318346755 | 0.245641327 |
| 原输入 / EOS评分 | −81.5 / −166 | −81.5 / −166 |

收益在正常路径保留。旧配对实验对应评分是−82/−167，且FT旧曲线采用旧评分环境；因此本次结果文件里的历史FT差值只作原始记录，**不能用新MAS 0.2456直接宣布胜过旧FT 0.2677**。不为此改FT、原模型或FA/FLA，不追加全变体测试。本次可声称的是当前DT与修复版在同一原评分条件下改善；此前独立配对MH结果及早段退步照常保留。

见[原评分结果](../../evidence/dt_normgate_production_MH_metrics_20260908_v1/results.json)和[独立复核](../../evidence/dt_normgate_production_MH_metrics_summary_20260908.json)。整个修复系列合计4次模型加载/初始化、10次完整DT、2次额外第0层配对候选、134次原评分、126次缓存标量读取，0次FT、0次生成、0条新数据。GPU任务用时合计约241秒，另CPU beta诊断5.59秒；不将分析、编译准备或归档时间隐去后称为总周转耗时。

本阶段关闭测试扩跑，保留显式候选入口。Goal仍开启：下一处优先MH第19层decoder（历史中段整层误差+4.8547），以第6层GDN（+4.3397）为同次定位对照，先测实际内部错配再选修法；整层边界不能归罪于FA内核。当前不声称已解决所有部分删除条件、独立泛化或真实batch。
