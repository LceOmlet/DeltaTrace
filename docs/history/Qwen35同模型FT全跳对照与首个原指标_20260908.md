# Qwen3.5 同模型 FT 全跳对照与首个原指标

同一Qwen3.5-9B上，DeltaTrace尚未表现出质量优势。已完成真实32层、异长B2的修正FT0—3，随后直接计算作者NI0的原needle恢复指标。没有增加样本、调方法或继续追逐微小精度差异。

| 固定方法 | NI0 needle恢复率 | 选中的gold token |
| --- | ---: | ---: |
| DeltaTrace content_P1 | 2.5% | 1/40 |
| 修正原生FT0 | 5.0% | 2/40 |
| 修正原生FT1 | 7.5% | 3/40 |
| 修正原生FT2 | 7.5% | 3/40 |
| 修正原生FT3 | 7.5% | 3/40 |

这是作者已经用过的NI0一例：310个可归因token中取前10%，共31个；整句needle映射得到44个gold token，其中40个在同一作者保留范围内。调用的是未经修改的作者evaluate_attr_recovery_skip_tokens，top_fraction=0.1。归因先完成并冻结，之后才读gold；没有向方法提供gold或删改命中标准。作者函数对负分取0，原始DeltaTrace有符号向量另存，不能把恢复率当作符号有效性。

两者分数都低，DeltaTrace还落后。这是需要正视的首个同模型负结果；单例不能推广到测试集，也不能用旧Qwen3-8B上的优势覆盖它。本轮未计算RISE/MAS，不能用恢复率推断删除忠实性胜负。模型名称已核验为共享Qwen3.5-9B，不能冒称Qwen3.5-8B或论文原表复现。

## 完整FT对照已执行

32层含24个GDN/FLA和8个标准注意力/FA，均消费同一实际checkpoint及与DeltaTrace相同的NI0/MH1固定轨迹。作者答案段和thinking段分别按605/368两条真实长度处理；FA/GDN内容伴随仍用真实原生前反向，默认模型未替换。FT用表示贡献，DeltaTrace用同一答案段log-prob，两种内部标量区别保留。

0—3跳沿用作者的thinking权重、BF16归一化、thinking占比递推和observation SUM。两条样本均用未经修改的作者hop控制函数对已计算的逐hop聚合向量做CPU复核，权重、占比和累计观察向量一致；这个复核不执行模型，也不是替代注意力。每层token分数、四轮权重/总量/累计结果及原生节点已保存。

对照是明确标注的修正原生内容FT，来源为官方e81b3be：纳入真实输出门控/归一化与卷积前来源，区别于原版结构遗漏。它没有改用DeltaTrace有限规则，也不能冒称未经修改的作者FT。原版源码与修正版差异保持可查。

## 资源与一次范围修复

首轮完成32层FT0后，在FT1发现批处理聚合器漏了作者j<sink_end+1的来源范围。FT0有一处约2×10⁻⁶的越界分数，本身不值得阻挡质量比较；修复限于我们新增聚合器的因果范围，没有修改作者框架或提高精度。重算了受影响的FT0第31层，其余31层分数逐字节复用，随后完成FT1—3。没有重放任何已完成的模型层。

用户要求此后把实际效果放在前面：微小舍入、非逐位一致及无实质影响的误差不再成为延迟原指标比较的理由。本阶段工程预算关闭，不追加这类实验。

首轮89.12秒、选择性续算38.32秒，共127.44秒，均在最终ZIP之前计时，包含失败工作。原始decoder共重放30次、复用2份捕获，保存约4.848GB实际层操作数和参数缓存；后续只复用并搬运这些数据。全部尝试共131次内容计算，相比计划128次增加3次，分别为重算FT0第31层以及FT1已做/失败的前两层，费用未隐藏。实际反向节点共33次FA、98次FLA和98次causal-conv。

首轮有名计时段58.91秒，续算24.01秒；其余为未分段导入、核验和诊断等。冷调用、缓存IO、捕获和FP64诊断仍在其中，不能把本次用作两种完整方法的公平稳态速度比较。CPU原needle函数只执行5次，零模型/GPU调用。所有进程已终止，结果包、哈希、失败记录和完整分数已保存。

## 下一决策

维持同两条作者轨迹、同五个已冻结方法，沿用作者20步EOS累计删除，实际B2运行原RISE/MAS。共同原输入/最终基线按输入字节去重，最多97次B2原生前向；保留全部逐样本曲线、分数密度、真实请求与总成本。这里是小预算模型扩展开发检查，不是独立确认。先看这些结果再决定方法修改，不因单例低分扩样本或继续修框架。

见[完整摘要](../../evidence/qwen35_FT32_summary_20260908.json)与[下一指标预算](../../configs/qwen35_original_metrics_budget_20260908.json)。目标仍是提出比FT更有效的方法，当前尚未达到，Goal保持active。
