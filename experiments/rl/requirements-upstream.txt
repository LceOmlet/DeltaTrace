# Install these into the existing DeltaTrace CUDA environment. Do not install
# the full verl-agent2 requirements file: it would replace the host's pinned
# torch/transformers/FlashAttention stack.
trl==1.13.0
peft==0.21.0
bitsandbytes==0.50.2
gym-sokoban==0.0.6
torchdata==0.11.0
pyserini==0.22.1
qwen-vl-utils==0.0.14
